import SwiftUI

/// Lekki opis stanu składnika do prezentacji (tekst, ikona, kolor) — pomocniczy dla UI.
struct sklStan {
	let id: sklStanEnum
	let tekst: String
	let image: String
	let kolor: Color
}
