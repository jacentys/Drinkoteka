
# Klucz service_role Supabase — nigdy nie commituj
scripts/.supabase_service_key

# Dane logowania konta testowego/reviewera (App Store) — nigdy nie commituj
scripts/.reviewer_credentials

# Backupy Supabase (mogą zawierać dane użytkowników)
scripts/backups/

# Klucz prywatny In-App Purchase (App Store Server API) — nigdy nie commituj
*.p8

# Lokalny stan Supabase CLI (link do projektu) — nie część repo
supabase/.temp/

# Ustawienia Claude Code per-komputer (np. autoMemoryDirectory) — nigdy nie commituj
.claude/settings.local.json

# Surowe screenshoty (źródło do docs/www/assets/screenshots/, ~33MB) — nie commitować
docs/www/assets/ScreenshotEN/
docs/www/assets/ScreenshotPL/
